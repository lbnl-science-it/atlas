#!/bin/bash

## don't run this script with sudo as pip warn against it

# sudo apt install python3-pip done separately

pip install -r requirements.txt
#pip install pandas
#pip install numpy  matplotlib  seaborn  time  copy
